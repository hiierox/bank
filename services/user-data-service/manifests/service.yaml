apiVersion: v1
kind: Service
metadata:
  name: user-data-service-kbatrakov
  namespace: test
  labels:
    student: kbatrakov
    app: user-data-service
spec:
  selector:
    app: user-data-service
    student: kbatrakov
  ports:
      port: 8000
      targetPort: 8000  