apiVersion: v1
kind: Service
metadata:
  name: scoring-service-{{ .Values.student.name }}
  labels:
    {{- include "scoring-service-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "scoring-service-chart.selectorLabels" . | nindent 4 }}
