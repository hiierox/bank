apiVersion: v1
kind: Service
metadata:
  name: flow-selection-service-kbatrakov
  namespace: test
  labels:
    student: kbatrakov
    app: flow-selection-service
spec:
  selector:
    app: flow-selection-service
    student: kbatrakov
  ports:
      - port: 8000
        targetPort: 8000
